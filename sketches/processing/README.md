# Processing Sketches

Processing Java-mode sketches live here.

Processing expects each sketch to sit in a folder with the same name as the main `.pde` file:

```text
sketch_name/sketch_name.pde
```

Install Processing from <https://processing.org/download>. VS Code can run these sketches with a Processing extension plus the Processing command-line runner.
