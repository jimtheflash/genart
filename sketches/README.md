# Sketches

Runnable artwork lives here, grouped by language.

Each sketch should be small enough to understand, easy to run, and clear about its main parameters. When a sketch produces files, prefer writing them to `outputs/<sketch-name>/`.
